KEY_PATH="~/Downloads/techx-oa-keypair.pem"
EC2_1_PUBLIC_IP="18.188.84.212"
EC2_2_PUBLIC_IP="18.118.103.228"
ALB_DNS="web-alb-1125866445.us-east-2.elb.amazonaws.com"

chmod 400 ~/Downloads/techx-oa-keypair.pem

ssh -i ~/Downloads/techx-oa-keypair.pem ubuntu@$EC2_1_PUBLIC_IP
sudo apt update
sudo apt install nginx -y
echo '<h1>Server 1</h1>' | sudo tee /var/www/html/index.html
sudo systemctl enable nginx
sudo systemctl restart nginx
curl http://localhost
exit

ssh -i ~/Downloads/techx-oa-keypair.pem ubuntu@$EC2_2_PUBLIC_IP
sudo apt update
sudo apt install nginx -y
echo '<h1>Server 2</h1>' | sudo tee /var/www/html/index.html
sudo systemctl enable nginx
sudo systemctl restart nginx
curl http://localhost
exit

curl http://$EC2_1_PUBLIC_IP
curl http://$EC2_2_PUBLIC_IP
curl http://$ALB_DNS

git add techx-aws-load-balancing-oa/README.md techx-aws-load-balancing-oa/commands.txt
git commit -m "Add AWS load balancing assignment"
git push -u origin main
